<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= motorcycle.name %> Details | Motorcycle Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header'); %>

    <main class="main-container">
        <div class="motorcycle-detail card">
            <div class="card-header bg-primary text-white">
                <div class="d-flex justify-content-between align-items-center">
                    <h1 class="mb-0"><%= motorcycle.name %></h1>
                    <a alt="<%= motorcycle.manufacturer.name %> logo" 
                        class="manufacturer-logo">></a>
                </div>
            </div>

            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <img src="<%= motorcycle.imageUrl %>" 
                             alt="<%= motorcycle.name %>" 
                             class="img-fluid rounded mb-4">
                    </div>
                    <div class="col-md-6">
                        <div class="specifications">
                            <h2 class="text-primary mb-4">Specifications</h2>
                            <ul class="list-group">
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Manufacturer:</span>
                                    <span><%= motorcycle.manufacturer.name %></span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Country:</span>
                                    <span><%= motorcycle.manufacturer.country %></span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Founded:</span>
                                    <span><%= motorcycle.manufacturer.founded %></span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Engine:</span>
                                    <span><%= motorcycle.engineCapacity %>cc</span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Category:</span>
                                    <span class="badge bg-secondary"><%= motorcycle.category %></span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Status:</span>
                                    <span class="badge <%= motorcycle.motorcycleStatus ? 'bg-success' : 'bg-danger' %>">
                                        <%= motorcycle.motorcycleStatus ? "Available" : "Discontinued" %>
                                    </span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between">
                                    <span class="fw-bold">Released:</span>
                                    <span><%= new Date(motorcycle.releaseDate).toLocaleDateString() %></span>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="features mt-4">
                    <h3 class="text-primary">Features</h3>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <% motorcycle.features.forEach(feature => { %>
                            <span class="feature-badge"><%= feature %></span>
                        <% }) %>
                    </div>
                </div>

                <div class="description mt-4">
                    <h3 class="text-primary">Description</h3>
                    <p class="mt-2"><%= motorcycle.description %></p>
                </div>
            </div>

            <div class="card-footer bg-light">
                <a href="/" class="btn btn-primary">
                    <i class="bi bi-arrow-left"></i> Back to Gallery
                </a>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</body>
</html>