<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header'); %>

    <main class="container mt-4">
        <h1 class="mb-4"><%= title %></h1>
        
        <div class="card mb-4">
            <div class="card-body">
                <form method="get" class="row g-3">
                    <div class="col-md-6">
                        <input type="text" name="search" class="form-control" 
                               placeholder="Search manufacturers..." value="<%= searchQuery %>">
                    </div>
                    <div class="col-md-3">
                        <select name="sortBy" class="form-select">
                            <option value="name" <%= sortBy === 'name' ? 'selected' : '' %>>Name</option>
                            <option value="country" <%= sortBy === 'country' ? 'selected' : '' %>>Country</option>
                            <option value="founded" <%= sortBy === 'founded' ? 'selected' : '' %>>Founded Year</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <select name="sortOrder" class="form-select">
                            <option value="asc" <%= sortOrder === 'asc' ? 'selected' : '' %>>Ascending</option>
                            <option value="desc" <%= sortOrder === 'desc' ? 'selected' : '' %>>Descending</option>
                        </select>
                    </div>
                    <div class="col-md-6 mt-2">
                        <button type="submit" class="btn btn-primary">Apply Filters</button>
                        <a href="/manufacturer" class="btn btn-outline-secondary ms-2">Reset</a>
                    </div>
                </form>
            </div>
        </div>

        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% manufacturers.forEach(manufacturer => { %>
                <div class="col">
                    <div class="card h-100">
                        <img src="<%= manufacturer.logoUrl %>" 
                             class="card-img-top p-3 manufacturer-logo" 
                             alt="<%= manufacturer.name %> logo">
                        <div class="card-body">
                            <h5 class="card-title"><%= manufacturer.name %></h5>
                            <p class="card-text">
                                <span class="badge bg-secondary"><%= manufacturer.country %></span>
                                <span class="d-block mt-2">Founded: <%= manufacturer.founded %></span>
                            </p>
                        </div>
                        <div class="card-footer bg-transparent">
                            <a href="/manufacturer/<%= manufacturer.id %>" class="btn btn-primary">
                                View Models
                            </a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </main>

    <%- include('partials/footer'); %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>